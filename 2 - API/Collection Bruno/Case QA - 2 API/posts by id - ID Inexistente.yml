info:
  name: posts by id - ID Inexistente
  type: http
  seq: 2

http:
  method: GET
  url: https://jsonplaceholder.typicode.com/posts/9999
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("O status do retorno é 404", function () {
            expect(res.getStatus()).to.equal(404);
        });

        test("O retorno é vazio", function () {
          const body = res.getBody();
          expect(body).to.be.empty;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
