{
  "name": "testes-api-case-qa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:vitest": "vitest run",
    "test:ui": "vitest --ui",
    "test:report": "vitest run --reporter=html --reporter=verbose"
  },
  "dependencies": {
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "@vitest/ui": "^2.0.5",
    "vitest": "^2.1.4"
  }
}